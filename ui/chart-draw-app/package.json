{
  "name": "chart-draw-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode development",
    "build": "vite build --mode production && mkdir -p dist/charting_library && cp -r charting_library/charting_library dist/charting_library/",
    "build:dev": "vite build --mode development && mkdir -p dist/charting_library && cp -r charting_library/charting_library dist/charting_library/",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "@react-oauth/google": "^0.13.4",
    "@stomp/stompjs": "^7.0.0",
    "lightweight-charts": "^4.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^10.1.0",
    "react-router-dom": "6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.2.73",
    "@types/react-dom": "^18.2.23",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
