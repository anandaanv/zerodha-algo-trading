spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/algotrading?autoreconnect=true
spring.datasource.username=anand
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update

hibernate.dialect.storage_engine=innodb
server.port=8080
#spring.jpa.show-sql=true
#spring.datasource.testOnBorrow=true
#spring.datasource.testWhileIdle=true
#spring.datasource.timeBetweenEvictionRunsMillis=60000
#spring.datasource.minEvictableIdleTimeMillis=30000
#spring.datasource.validationQuery=SELECT 1
#spring.datasource.max-active=15
#spring.datasource.max-idle=10
#spring.datasource.max-wait=8000

spring.datasource.hikari.keepaliveTime=60000
spring.datasource.hikari.idleTimeout=120000
spring.datasource.hikari.maxLifetime=600000
spring.datasource.hikari.maximum-pool-size=30
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connectionTimeout=30000


# Secrets moved to DB (app_secrets) or environment placeholders (overridden by DB on startup)
kite.api.key=${KITE_API_KEY:}
kite.api.user=${KITE_API_USER:}
kite.api.secret=${KITE_API_SECRET:}

# Multi-app Kite pool: set to number of Kite developer apps you have registered
# Each app multiplies the API rate limit (3 req/sec each) and WebSocket capacity
# App N credentials stored in app_secrets with keys: kite.app.N.api.key / .secret / .user.id
# OR in kite_connect_settings DB with id=N+1 (id=1 is legacy app 0)
kite.apps.count=${KITE_APPS_COUNT:1}

# JWT secret (must match the value used in JwtRequestFilter)
jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyReplaceInProduction}

# Dhan API Configuration
# Enable Dhan integration (default: false)
dhan.enabled=${DHAN_ENABLED:true}
dhan.client.id=${DHAN_CLIENT_ID:}
dhan.access.token=${DHAN_ACCESS_TOKEN:}
dhan.user.id=${DHAN_USER_ID:}
dhan.api.base-url=${DHAN_API_BASE_URL:https://api.dhan.co/v2}

# Remote Sync Configuration for Chart State
remote.sync.api.url=${REMOTE_SYNC_API_URL:}
remote.sync.username=${REMOTE_SYNC_USERNAME:}
remote.sync.password=${REMOTE_SYNC_PASSWORD:}

spring.application.admin.enabled=true
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# Matplotlib chart configuration
matplotlib.python.script.path=src/main/python/matplotlib_chart.py
matplotlib.temp.directory=/tmp
matplotlib.gateway.port=25333

# OpenAI configuration
openai.baseUrl=${OPENAI_BASE_URL:https://api.openai.com/v1}
openai.apiKey=${OPENAI_API_KEY:}
openai.key=${OPENAI_API_KEY:}
spring.ai.openai.api-key=${OPENAI_API_KEY:placeholder-not-configured}
spring.ai.openai.chat.options.temperature=1
spring.ai.openai.chat.options.timeout=60000
spring.ai.retry.max-attempts=2
openai.model=${OPENAI_MODEL:gpt-5.3-turbo}

# Charts configuration
charts.output.directory=/tmp/charts
charts.temp.directory=/tmp/charts/temp
charts.use.tradingview=true

# Browser Pool Configuration
charts.browser.pool.size=3
charts.browser.timeout=30

# WhatsApp configuration
whatsapp.token=${WHATSAPP_TOKEN:}
whatsapp.recipients=${WHATSAPP_RECIPIENTS:}
alerts.whatsapp.types=SWING_BUY,SWING_SELL,ASTA_BUY,ASTA_SELL
whatsapp.phone-number-id=${WHATSAPP_PHONE_NUMBER_ID:}
whatsapp.graph.api.version=${WHATSAPP_GRAPH_API_VERSION:v22.0}

patterns.trendlines.enabled=true
charts.visibleBars.default=1000

# Optional: legacy key form
openai.base-url=${OPENAI_BASE_URL:https://api.openai.com/v1}

# Market Data Provider Configuration
# Options: zerodha (direct Zerodha Kite API), database (local DB cache)
# Default: database (recommended for production use)
# To use Zerodha: export MARKET_DATA_PROVIDER=zerodha
market.data.provider=${MARKET_DATA_PROVIDER:database}

spring.devtools.restart.exclude=org/jetbrains/kotlin/**,kotlin-*.jar,META-INF/services/javax.script.ScriptEngineFactory
spring.devtools.restart.additional-exclude=com/dtech/algo/screener/**,com/dtech/algo/screener/dsl/**

spring.devtools.restart.enabled=false

# Google OAuth2 Configuration
# Get these values from Google Cloud Console: https://console.cloud.google.com/apis/credentials
google.oauth2.client-id=${GOOGLE_OAUTH2_CLIENT_ID:}
google.oauth2.client-secret=${GOOGLE_OAUTH2_CLIENT_SECRET:}

# Co-Pilot configuration
copilot.encryption.key=${COPILOT_ENCRYPTION_KEY:copilot-default-key-change-in-prod}
copilot.investigation.default-validity-minutes=60
copilot.async.core-pool-size=4
copilot.async.max-pool-size=8
copilot.async.queue-capacity=50

# Logging configuration
logging.file.name=${catalina.base:./logs}/logs/tradeapi.log
logging.level.com.dtech.algo.service.TradingViewChartService=DEBUG
logging.level.com.dtech.kitecon.web.copilot=INFO
logging.level.com.dtech.kitecon.service.copilot=DEBUG

# ── Subscription UOW Worker ──────────────────────────────────────────────────
# Startup grace period before first tick — gives app time to fully init
# and prevents all 1200+ UOWs firing simultaneously on startup
data.uow.initial-delay=60000
# Max UOWs dispatched per tick — each is @Async+@Transactional (holds a DB connection)
# Keep low (<=pool-size/4) to avoid exhausting the pool (max-pool-size=30)
data.uow.batchSize=8

# ── Trade Monitor Worker ──────────────────────────────────────────────────────
# Set to false only after BrokerOrderService is wired to Kite Connect
trade.monitor.dryrun=true
# Worker polling interval in milliseconds (default 5 minutes)
trade.monitor.interval=300000
# Notional value per futures lot in INR (default ₹10L)
trade.monitor.notional=1000000
# Margin deployed per lot in INR (default ₹3L at 30% of ₹10L notional)
trade.monitor.margin=300000
# Default entry window validity in hours (signal expires if entry not triggered)
trade.monitor.entry.validity.hours=4
logging.level.com.dtech.kitecon.trade=INFO
logging.level.com.dtech.kitecon.kite=INFO

# Trading portfolio and scanner config
trade.portfolio.value=500000
trade.scanner.watching.tf=1h
trade.scanner.confirm.tf=15m

# ML Trade Filter Configuration
trade.filter.service.url=http://localhost:5001
trade.filter.enabled=true
trade.filter.threshold=0.81

# Impulse strategy configuration
impulse.enabled=false
impulse.threshold=0.40
impulse.prediction.url=http://localhost:8501/predict-batch
impulse.max.trades.per.symbol=2
impulse.otm.distance.pct=3
impulse.paper.trade=true


# ── Impulse Live Trading Config ──
# impulse.enabled=true        # UNCOMMENT ON MONDAY MORNING
# impulse.paper.trade=false   # UNCOMMENT FOR LIVE ORDERS
impulse.capital.per.trade=25000
impulse.otm.distance.pct=3
impulse.simulation.segment=EQ
