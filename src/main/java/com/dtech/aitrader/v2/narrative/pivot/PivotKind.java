package com.dtech.aitrader.v2.narrative.pivot;

/**
 * Enum representing the type of pivot detected in a numeric series.
 */
public enum PivotKind {
    /** Local maximum (peak). */
    PEAK,
    /** Local minimum (trough). */
    TROUGH
}
